export const paymentStatuses = ["unpaid", "partial", "paid", "returned", "cancelled"] as const;
export const courierStatuses = ["pending", "dispatched", "in_transit", "delivered", "returned", "failed"] as const;

